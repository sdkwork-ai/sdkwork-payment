# standalone.demo — independent demonstration deployment, fully isolated from dev/test/staging/prod.
# Provisioned as its own deployment: dedicated PostgreSQL schema `sdkwork_payment_demo` (table
# prefix `commerce_`) and a dedicated Redis logical DB index so that no cache key (rate-limit,
# session, idempotency) ever collides with another shared-redis environment.
SDKWORK_PAYMENT_DEPLOYMENT_PROFILE=standalone
SDKWORK_PAYMENT_ENVIRONMENT=demo
SDKWORK_ENV=demo
SDKWORK_PAYMENT_PROFILE_ID=standalone.demo
SDKWORK_PAYMENT_APPLICATION_PUBLIC_HTTP_URL=https://payment-demo.sdkwork.com
SDKWORK_PAYMENT_APPLICATION_PUBLIC_INGRESS_BIND=127.0.0.1:3900


# Region deployment dimension (REGION_SPEC.md): orthogonal to deployment profile and environment.
SDKWORK_PAYMENT_REGION_CODE=cn
SDKWORK_DATABASE_SEED_LOCALE=zh-CN
