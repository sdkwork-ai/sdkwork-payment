FROM rust:1-bookworm AS builder
WORKDIR /src
COPY . .
RUN cargo build --release -p sdkwork-api-payment-standalone-gateway

FROM debian:bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends ca-certificates && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY --from=builder /src/target/release/sdkwork-api-payment-standalone-gateway /usr/local/bin/sdkwork-api-payment-standalone-gateway
ENV PAYMENT_API_BIND=0.0.0.0:18094
EXPOSE 18094
CMD ["sdkwork-api-payment-standalone-gateway"]
